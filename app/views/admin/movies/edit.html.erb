<div class="container"><br>
  <%= link_to t('admin.movies.back'), admin_movies_path, class: "btn btn-secondary position-absolute top-1 start-1 mt-2 ms-2" %>
  <br>
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-9">
        <div class="card shadow-sm">
          <div class="card-body">
            <h1 class="card-title text-center"><%=t('admin.movies.edit')%></h1>
            <%= render "form", movie: @movie %>
            <br>
          </div>
        </div>
      </div>
    </div>
  </div>
</div><br>
